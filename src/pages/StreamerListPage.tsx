import React from 'react';

const StreamerListPage: React.FC = () => {
  return (
    <div>
      <h1>Streamer List Page</h1>
      {/* ここに配信者一覧を表示するコンポーネントを追加 */}
    </div>
  );
};

export default StreamerListPage;